[project]
name = "volumehandchanger"
version = "0.1.0"
description = ""
authors = [
    {name = "Dimekadze",email = "newakk74@mail.ru"}
]
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "opencv-python (>=5.0.0.93,<6.0.0.0)",
    "mediapipe (>=1.0.1,<2.0.0)",
    "pulsectl (>=24.12.0,<25.0.0)"
]

[tool.poetry]
packages = [{include = "volumehandchanger", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
